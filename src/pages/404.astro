---
import MainLayout from "src/Layouts/MainLayout.astro";
import type { Language } from "../types/language.type";
import es from "../i18n/es";
import en from "../i18n/en";

export function getStaticPaths() {
  return [{ params: { lang: "es" } }, { params: { lang: "en" } }];
}

const lang = Astro.params.lang as Language;
const translations = { es, en };
const t = translations[lang];
---

<div
  class="flex flex-col items-center justify-center select-none h-[70vh] text-center gap-6"
>
  <h1 class="text-9xl font-extrabold text-white tracking-widest">404</h1>
  <div class="bg-[#FF6A3D] px-2 text-sm rounded rotate-12 absolute">
    Page Not Found
  </div>
  <button class="mt-5">
    <a href="/" class="relative inline-block text-sm font-medium">
      <span
        class="px-3 py-2 rounded-lg bg-neutral-800 cursor-pointer hover:bg-neutral-700 transition-all select-none"
      >
        Ir a la página principal
      </span>
    </a>
  </button>
</div>
